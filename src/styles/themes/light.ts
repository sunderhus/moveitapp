import {Colors} from '../global';

export default {
  title: `${Colors.title}`,

  colors: {
    primary: Colors.red,
    secundary: `${Colors.green}`,

    background: `${Colors.background}`,
    text: `${Colors.text}`,
  },
};
