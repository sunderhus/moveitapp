import {Colors} from '../global';

export default {
  title: `${Colors.white}`,

  colors: {
    primary: Colors.blueDark,
    secundary: Colors.green,

    background: Colors.title,
    text: Colors.white,
    title: Colors.white,
  },
};
